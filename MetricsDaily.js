// Backward-compatible alias. All metric ingestion and reads now share the
// canonical organization-scoped MetricDaily model and metrics_daily collection.
export { default } from "./MetricDaily.js";
